apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mathjax.fullname" . }}-test-connection"
  labels:
    {{- include "mathjax.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mathjax.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
