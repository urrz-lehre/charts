apiVersion: v1
kind: Service
metadata:
  name: {{ include "mathjax.fullname" . }}
  labels:
    {{- include "mathjax.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mathjax.selectorLabels" . | nindent 4 }}
