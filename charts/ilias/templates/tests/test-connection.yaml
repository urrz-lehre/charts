apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ilias.fullname" . }}-test-connection"
  labels:
    {{- include "ilias.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ilias.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
